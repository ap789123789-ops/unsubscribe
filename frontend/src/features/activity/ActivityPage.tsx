export function ActivityPage() {
  return <main className="page" id="main-content"><p className="context-line">Unsubscribe activity</p><h1>Action results will appear here.</h1><p className="page-intro">Submitted and confirmed are kept separate so an uncertain result never looks complete.</p></main>
}

